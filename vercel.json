{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/web && npm install --legacy-peer-deps && npx prisma generate && npx next build",
  "outputDirectory": "apps/web/.next"
}
