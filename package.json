{
  "name": "meu-concierge",
  "version": "1.0.0",
  "private": true,
  "description": "Travel concierge platform",
  "engines": {
    "node": "20.x",
    "pnpm": "9.x"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "cd apps/web && npm install && npx prisma generate && npx next build",
    "start": "cd apps/web && npx next start",
    "dev": "cd apps/web && npm run dev"
  },
  "workspaces": [
    "apps/web"
  ]
}
